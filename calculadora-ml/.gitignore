node_modules/
dist/

# cache local do mapa de calor (visitas por produto) — da pra regerar
.cache-visitas.json

# NUNCA versione segredos nem tokens
.env
.tokens.json
.user-token.json

# fotos diarias do ranking "Em alta" (dados gerados)
dados/

# cache local do job de categorias
.cache-categorias.json
